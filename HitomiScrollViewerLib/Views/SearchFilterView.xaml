<?xml version="1.0" encoding="utf-8"?>
<StackPanel
    x:Class="HitomiScrollViewerLib.Views.SearchFilterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:views="using:HitomiScrollViewerLib.Views"
    xmlns:vms="using:HitomiScrollViewerLib.ViewModels"
    Orientation="Vertical">
    <TextBlock Text="{x:Bind ViewModel.GalleryLanguageText}"/>
    <TextBlock Text="{x:Bind ViewModel.GalleryTypeText}"/>
    <ItemsRepeater ItemsSource="{x:Bind ViewModel.SearchFilterTagsRepeaterVMs}">
        <ItemsRepeater.ItemTemplate>
            <DataTemplate x:DataType="vms:SearchFilterTagsRepeaterVM">
                <views:SearchFilterTagsRepeater ViewModel="{x:Bind}"/>
            </DataTemplate>
        </ItemsRepeater.ItemTemplate>
    </ItemsRepeater>
    <TextBlock Text="{x:Bind ViewModel.SearchTitleText}"/>
</StackPanel>
