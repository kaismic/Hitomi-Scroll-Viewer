<?xml version="1.0" encoding="utf-8"?>
<Grid
    x:Class="HitomiScrollViewerLib.Controls.SearchPageComponents.InputValidation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HitomiScrollViewerLib.Controls.SearchPageComponents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <TextBox x:Name="InputTextBox"
                 Grid.Row="0"
                 MaxWidth="{x:Bind MaxWidth, Mode=OneWay}"
                 Width="{x:Bind Width, Mode=OneWay}"
                 />
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="ErrorMsgTextBlock"
                       Grid.Column="0"
                       Foreground="Red"
                       HorizontalAlignment="Left"/>
        <TextBlock x:Name="LengthDisplayTextBlock"
                       Grid.Column="1"
                       HorizontalAlignment="Right"/>
    </Grid>
</Grid>
