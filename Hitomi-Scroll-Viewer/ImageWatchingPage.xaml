<Page
    x:Class="Hitomi_Scroll_Viewer.ImageWatchingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    PreviewKeyDown="ImageWatchingPage_PreviewKeyDown">
    <Grid>
        <FlipView x:Name="ImageFlipView"
                  FocusVisualPrimaryThickness="0" IsTabStop="True"
                  SelectionChanged="ImageFlipView_SelectionChanged">
        </FlipView>
        <CommandBar x:Name="TopCommandBar"
                    Background="{ThemeResource LayerFillColorAltBrush}"
                    ClosedDisplayMode="Compact"
                    OverflowButtonVisibility="Collapsed"
                    HorizontalAlignment="Center"
                    IsOpen="True">
            <AppBarButton x:Name="GoBackBtn"
                          MinWidth="50"
                          Icon="Back"
                          Label="Go Back"
                          IsTabStop="False"/>
            <AppBarSeparator MinWidth="30"/>
            <AppBarButton x:Name="RefreshBtn"
                          MinWidth="50"
                          Icon="Refresh"
                          Label="Refresh"
                          Click="RefreshBtn_Clicked"
                          IsTabStop="False"/>
            <AppBarSeparator MinWidth="30"/>
            <AppBarElementContainer>
                <Slider x:Name="AutoScrollIntervalSlider"
                        MinWidth="240"
                        IsTabStop="False"
                        Minimum="{x:Bind AUTO_SCROLL_INTERVAL_RANGE.Start.Value}"
                        Maximum="{x:Bind AUTO_SCROLL_INTERVAL_RANGE.End.Value}"
                        TickFrequency="{x:Bind AUTO_SCROLL_INTERVAL_FREQ}"
                        StepFrequency="{x:Bind AUTO_SCROLL_INTERVAL_FREQ}">
                    <Slider.Header>
                        <TextBlock Text="Auto Scroll Interval (seconds)"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                    </Slider.Header>
                </Slider>
            </AppBarElementContainer>
            <AppBarSeparator MinWidth="30"/>
            <AppBarToggleButton x:Name="AutoScrollBtn"
                                MinWidth="90"
                                Icon="Play"
                                Label="Start Auto Scrolling"
                                IsTabStop="False"/>
            <AppBarToggleButton x:Name="LoopBtn"
                                Click="LoopBtn_Clicked"
                                IsChecked="{x:Bind _isLooping, Mode=TwoWay}"
                                Label="Toggle Loop"
                                Icon="RepeatAll"
                                MinWidth="90"
                                IsTabStop="False"
                                VerticalAlignment="Center"/>
            <AppBarSeparator MinWidth="30"/>
            <AppBarElementContainer>
                <ComboBox x:Name="ScrollDirectionSelector" ItemsSource="{x:Bind ORIENTATION_NAMES}"
                          SelectionChanged="ScrollDirectionSelector_SelectionChanged"
                          MinWidth="150"
                          IsTabStop="False">
                    <ComboBox.Header>
                        <TextBlock Text="Scroll Direction"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                    </ComboBox.Header>
                </ComboBox>
            </AppBarElementContainer>
            <AppBarSeparator MinWidth="30"/>
            <AppBarElementContainer>
                <ComboBox x:Name="ViewDirectionSelector" ItemsSource="{x:Bind VIEW_DIRECTION_NAMES}"
                          SelectionChanged="ViewDirectionSelector_SelectionChanged"
                          MinWidth="160"
                          IsTabStop="False">
                    <ComboBox.Header>
                        <TextBlock Text="View Direction"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                    </ComboBox.Header>
                </ComboBox>
            </AppBarElementContainer>
            <AppBarSeparator MinWidth="30"/>
            <AppBarElementContainer>
                <ComboBox x:Name="PageNavigator"
                          SelectionChanged="PageNavigator_SelectionChanged"
                          MinWidth="120"
                          IsTabStop="False">
                    <ComboBox.Header>
                        <TextBlock Text="Go to page"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   HorizontalAlignment="Center"/>
                    </ComboBox.Header>
                </ComboBox>
            </AppBarElementContainer>
        </CommandBar>
        <Border x:Name="PageNumDisplay"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                CornerRadius="16"
                BorderThickness="1"
                Visibility="Collapsed"
                HorizontalAlignment="Center" VerticalAlignment="Top"
                Margin="0, 128, 0, 0"
                Padding="8"
                IsTabStop="False"
                IsTapEnabled="False">
            <TextBlock x:Name="PageNumText" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <Viewbox x:Name="ActionIndicator"
                 Width="500"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Opacity="0"
                 IsHitTestVisible="False"
                 IsTabStop="False"
                 IsTapEnabled="False">
            <Viewbox.Resources>
                <Storyboard x:Name="FadeOutStoryboard">
                    <DoubleAnimation Storyboard.TargetName="ActionIndicator"
                                     Storyboard.TargetProperty="Opacity"
                                     From="1" To="0"
                                     Duration="0:0:1">
                    </DoubleAnimation>
                </Storyboard>
            </Viewbox.Resources>
            <Grid>
                <SymbolIcon x:Name="ActionIndicatorSymbolIcon"
                            Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                <FontIcon x:Name="ActionIndicatorFontIcon" FontFamily="{StaticResource SymbolThemeFontFamily}"
                          Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                          FontSize="32"/>
            </Grid>
        </Viewbox>
    </Grid>
</Page>
