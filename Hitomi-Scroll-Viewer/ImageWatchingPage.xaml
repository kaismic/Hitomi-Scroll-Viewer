<Page
    x:Class="Hitomi_Scroll_Viewer.ImageWatchingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hitomi_Scroll_Viewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="MainGrid">
        <CommandBar x:Name="TopCommandBar" ClosedDisplayMode="Hidden" OverflowButtonVisibility="Collapsed" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <AppBarButton x:Name="BookmarkBtn"
                          x:FieldModifier="public"
                          IsEnabled="False"
                          Width="300"
                          Icon="Save"
                          />
            <AppBarSeparator Width="50"/>
            <AppBarElementContainer>
                <Slider x:Name="ImageSizeScaleSlider"
                        StepFrequency="0.1"
                        TickFrequency="0.1"
                        Value="0.5"
                        Minimum="0.1"
                        Maximum="1.5"
                        Width="240"
                        Header="Image Size Scale"
                        FontSize="14"
                        HorizontalAlignment="Center"
                        ValueChanged="ChangeImageSize"/>
            </AppBarElementContainer>
            <AppBarSeparator Width="50"/>
            <AppBarElementContainer>
                <Slider
                        StepFrequency="1"
                        TickFrequency="1"
                        Value="1"
                        Minimum="0"
                        Maximum="10"
                        Width="240"
                        Header="Scroll Speed"
                        FontSize="14"
                        HorizontalAlignment="Center"
                        ValueChanged="SetScrollSpeed"/>
            </AppBarElementContainer>
            <AppBarSeparator Width="50"/>
            <AppBarToggleButton Label="Loop" Icon="RepeatAll" Click="HandleLoopToggleBtnClick"/>
        </CommandBar>
        <ScrollViewer x:Name="MainScrollViewer">
            <StackPanel x:Name="ImageContainer" Orientation="Vertical"/>
        </ScrollViewer>
    </Grid>
</Page>
